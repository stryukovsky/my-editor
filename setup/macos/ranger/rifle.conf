ext cue, has audacious, flag f = setsid audacious "$@" </dev/null >/dev/null 2>&1 &
ext sh =  nvim "$@" 
mime ^text/, label editor =  nvim "$@" 
mime ^application/pdf, has open = open "$@"
mime ^application/, label editor =  nvim "$@" 

mime ^video,       has mpv,      flag f =  setsid mpv -- "$@" </dev/null >/dev/null 2>&1 &
mime ^image/, has open = open "$@"
mime ^audio/, has open = open "$@"

# ARCHIVES
# avoid password prompt by providing empty password
ext 7z, has 7z = 7z -p l "$@" | "$PAGER"
#
# # This requires atool
ext ace|ar|arc|bz2?|cab|cpio|cpt|deb|dgc|dmg|gz,     has atool = atool --list --each -- "$@" | "$PAGER"
ext iso|jar|msi|pkg|rar|shar|tar|tgz|xar|xpi|xz|zip, has atool = atool --list --each -- "$@" | "$PAGER"
ext 7z|ace|ar|arc|bz2?|cab|cpio|cpt|deb|dgc|dmg|gz,  has atool = atool --extract --each -- "$@"
ext iso|jar|msi|pkg|rar|shar|tar|tgz|xar|xpi|xz|zip, has atool = atool --extract --each -- "$@"

# WEB
ext x?html?, has open = open "$@"

# MISC
mime ^ranger/x-terminal-emulator, has alacritty = alacritty -e "$@"
mime ^font, has fontforge, flag f = fontforge "$@"

