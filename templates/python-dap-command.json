{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "LangGraph backend",
      "type": "debugpy",
      "request": "launch",
      "program": "${workspaceFolder}/.venv/bin/langgraph",
      "cwd": "${workspaceFolder}",
      "args": ["dev"]
    }
  ]
}

