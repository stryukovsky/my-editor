{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "LangGraph Dev (uv run)",
      "type": "debugpy",
      "request": "launch",
      "module": "langgraph",
      "args": ["dev"],
      "cwd": "${workspaceFolder}"
    }
  ]
}

